SELECT category, SUM(amount) AS total_spent
FROM Expenses
GROUP BY category;

SELECT category, AVG(amount) AS average_expense
FROM Expenses
GROUP BY category;

SELECT category, SUM(amount) AS total_spent
FROM Expenses
GROUP BY category
ORDER BY total_spent DESC
LIMIT 3; -- Change the number 3 to your desired number of top categories
